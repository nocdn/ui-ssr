import { ComponentLayout } from "@/components/component-layout"

export const metadata = { title: "Reasoning Traces" }

export default function ReasoningTracesPage() {
  return (
    <ComponentLayout
      componentName="Reasoning Traces"
      description="A component that displays reasoning traces (thoughts), for a large language model."
      comingSoon={true}
    />
  )
}
